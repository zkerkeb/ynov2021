import React from 'react';
import PropTypes from 'prop-types';

const Characters = props => {
console.log("🚀 ~ file: characters.js ~ line 5 ~ props", props)
    return (
        <div>
            <p>Characters</p>
        </div>
    );
};

Characters.propTypes = {
    
};

export default Characters;